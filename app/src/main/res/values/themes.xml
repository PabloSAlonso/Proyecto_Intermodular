<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Klyer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/blue_primary</item>
        <item name="materialButtonStyle">@style/SquareButton</item>
    </style>

    <style name="Theme.Klyer" parent="Base.Theme.Klyer" />

    <style name="CircleImage">
        <item name="cornerSize">50%</item>
    </style>

    <!-- Square button style -->
    <style name="SquareButton" parent="Widget.Material3.Button">
        <item name="cornerRadius">8dp</item>
    </style>
</resources>
